'use client';

// Stub: VocabularyDrawer — "Add to vocabulary" button on WordDetailModal (premium)
// This component will be implemented in a future sprint.

export function VocabularyDrawer() {
  if (process.env.NODE_ENV === 'development') {
    console.warn('VocabularyDrawer is a stub component — not yet implemented.');
  }
  return null;
}
